# Backend configuration
# Create .env in this folder or set env vars when running
PORT=4000
# Optional: set a webhook.site URL to receive webhook POSTs when jobs complete
# Example: WEBHOOK_URL=https://webhook.site/your-id
WEBHOOK_URL=
